import React from 'react';
import Card from './Card';

const List = ({ title }) => {
  return (
    <div>
      <p>{title}</p>
      {/* <Card />
      <Card />
      <Card />
      <Card /> */}
    </div>
  );
};

export default List;
